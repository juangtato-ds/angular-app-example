import { ModalDialogDirective } from './modal-dialog.directive';

describe('ModalDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new ModalDialogDirective();
    expect(directive).toBeTruthy();
  });
});
