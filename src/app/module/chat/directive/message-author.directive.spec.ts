import { MessageAuthorDirective } from './message-author.directive';

describe('MessageAuthorDirective', () => {
  it('should create an instance', () => {
    const directive = new MessageAuthorDirective();
    expect(directive).toBeTruthy();
  });
});
